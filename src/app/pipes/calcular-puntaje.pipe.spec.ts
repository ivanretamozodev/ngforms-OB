import { CalcularPuntajePipe } from './calcular-puntaje.pipe';

describe('CalcularPuntajePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularPuntajePipe();
    expect(pipe).toBeTruthy();
  });
});
